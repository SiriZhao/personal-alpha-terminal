# LLM Alpha Research Report

Final research state: `NOT_CERTIFIABLE`; factor state: `SHADOW`.

No real IC, Rank IC, regime stability, return spread or post-cost Alpha result is
reported. The required survivorship-safe market history is still unavailable, and
there is no certified historical PIT text/event package. Opening locked OOS under
those conditions would contaminate the research process.

The implemented research path supports PIT replay, cross-sectional event factors,
benchmark-relative outcomes, empirical-Bayes shrinkage, Brier/log-loss/ECE
calibration and a 252-session promotion minimum. Controlled fixtures demonstrate
the mechanics and anti-leakage invariants, but are explicitly excluded from Alpha
evidence. The next valid experiment begins only after both market and text datasets
are certified and split identities are frozen.
