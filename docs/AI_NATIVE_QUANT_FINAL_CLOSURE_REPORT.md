# AI-Native Quant Final Closure Report

Date: 2026-08-12

## Outcome

The project now has a real DeepSeek-backed, provider-neutral structured intelligence
vertical slice. Real authentication and inference passed through the production
provider and audited gateway. Event output is typed, versioned, cached and PIT
filtered; request/response hashes, document identities, cutoff, token usage, latency,
cost and validation state are recorded. Daily evidence contains an explicit
`LLM_INTELLIGENCE` stage and persists SHADOW factor snapshots when certified events
exist.

The classical Quant Core remains the only decision path. `llm_event_intensity` is
SHADOW with zero production influence. LLM-derived probability research uses
benchmark-relative outcomes, chronological labels, empirical-Bayes shrinkage and
OOS calibration, but cannot affect expected return, weights or recommendations
without an exact approval artifact. Provider/schema/data failure safely falls back
to the classical chain.

## Honest certification state

- DeepSeek connectivity: `PASS`
- structured output / provenance infrastructure: `PASS`
- software PIT and prompt-injection controls: `PASS`
- historical market research data: `NOT_CERTIFIABLE`
- historical PIT text/event data: `NOT_CERTIFIABLE`
- LLM factor production status: `SHADOW`
- Champion/Challenger locked OOS: `NOT_RUN`
- LLM incremental after-cost Alpha: `UNAVAILABLE`
- production promotion: none

The absence of OOS metrics is deliberate: reporting fixture performance would be
fabrication. Current strategy production approval is unchanged, the manual portfolio
and risk boundaries are unchanged, and no auto-execution or paper account was added.

## Final validation

- real DeepSeek minimal inference: `PASS`; structured gateway validation `VALID`;
- real DeepSeek event extraction on an isolated `TEST_FIXTURE`: `PASS`; typed
  `EARNINGS` event produced and never persisted as market evidence;
- provider-disabled failure drill: `PASS`; `LLM_INTELLIGENCE` became
  `OPTIONAL_UNAVAILABLE` and the classical result stayed
  `VALID_ANALYSIS_NON_ACTIONABLE`;
- live daily run with `main`: DATA/PIT/FEATURE/FACTOR `PASS`, LLM
  `PASS_DEGRADED/SHADOW`, SIGNAL `FAIL_BLOCKING`, actions `0`;
- repeated daily input: classification, data identity and config hash reproduced;
- Ruff: `PASS`; strict mypy: `PASS` across 366 source files;
- full pytest: `659 passed`.

## Remaining blockers

1. licensed/certified survivorship-safe historical market data;
2. certified PIT historical filings/transcripts/news with immutable availability;
3. frozen train/validation/embargo/252+ locked-OOS study with benchmark, costs,
   calibration and ablation.

Only after those blockers are removed may an individual LLM factor be promoted.
