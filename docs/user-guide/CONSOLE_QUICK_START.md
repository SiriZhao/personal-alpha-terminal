# Console quick start

1. 创建 Python 环境并安装项目依赖。
2. 运行全量测试确认本地环境一致。
3. 启动兼容 CLI/TUI 入口。
4. 在数据中心检查 Provider、快照、PIT 与质量门禁。
5. 数据不足时，Daily Decision 显示 `No Decision Generated`，这是预期的安全行为。
6. 历史回测和研究输出必须明确数据版本、模型版本、时间截止点与限制。
7. 用户对研究候选只能接受、拒绝或观察；系统不会创建模拟或真实订单。

本轮不构建 EXE。真实历史数据认证完成前，不得将输出解释为生产投资结论。
